import Vue from 'vue'
import { expect } from 'chai'
import mdiVue from './main.js'

describe('mdi-vue', () => {
  describe('telemetry', () => {
    it('should run a successful test', () => {
      expect(true).to.equal(true)
    })

    // it('should ')
  })
})